using UnityEngine;

/// <summary>
/// 기존 호환용 — 실제 동작은 AutoLocalizePanel과 동일.
/// 이미 옵션 패널에 이 컴포넌트가 붙어 있는 경우를 위해 유지.
/// 새 UI에는 AutoLocalizePanel 사용 권장.
/// </summary>
public class OptionsPanelAutoLocalize : AutoLocalizePanel { }
